import { Layout } from '../../components/Layout'
import './script'
import './style.css'

export const ContactView = (props) => {
    return (
        <Layout>
            <h1> Contact! </h1>
        </Layout>
    )
}